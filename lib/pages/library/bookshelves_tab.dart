import 'package:flutter/material.dart';

class BookshelvesTab extends StatelessWidget {
  const BookshelvesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("bookshelves"),
      ),
    );
  }
}
